= render 'layouts/html-header'
%body
	= render 'layouts/header'
	.container
		- flash.each do |key, value|
			%div{:class=> "alert alert-#{key}"}
				%p= value
		= yield
		= render 'layouts/footer'
		= debug(params) if Rails.env.development?

	= render 'layouts/html-footer'